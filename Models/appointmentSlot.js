export default class AppointmentSlot {
    constructor(id, serviceId, date, timeList){
        this.id = id 
        this.serviceId = serviceId
        this.date = date 
        this.timeList = timeList
    }
}